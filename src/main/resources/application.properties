# application.properties — FINAL CORRECT VERSION
spring.application.name=ez
server.port=8080

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ez_chat?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Logging
logging.level.com.example=DEBUG
logging.level.org.springframework.web.socket=INFO

# ✅ EMAIL — use spring.mail.* (Spring Boot auto-configures JavaMailSender)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=izharali4924@gmail.com
spring.mail.password=jjdb rbsd njgn fxzu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
cms.upload.dir=uploads
cms.upload.base-url=http://localhost:8080/uploads

# jwt work here
ez.jwt.secret=MySuperSecretJwtKeyForAuthentication2026ABC123456
ez.jwt.expiration-ms=86400000

# See real error
logging.level.org.hibernate.SQL=DEBUG